spring.application.name=ApiRest
server.port=9090

# Database configuration for MySQL
spring.datasource.url= jdbc:mysql://localhost:3307/APIREST?serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=191951212Migasa
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA) configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
# Options: create, create-drop, update, validate, none
spring.jpa.hibernate.ddl-auto=update
# Displays SQL queries in the console
spring.jpa.show-sql=true
# Formats the SQL output
spring.jpa.properties.hibernate.format_sql=true

